<%= form_for :game, :url => games_path do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>

  <%= form.hidden_field :turn_id, :value => 0 %>
  <%= form.hidden_field :log, :value => "" %>

  <%= form.submit "Create game", class: 'button' %>
<% end %>
